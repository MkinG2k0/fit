# Review package
BASE: aa93292d798095129003e634f09ac5efd89189d7
HEAD: 05c94c22c0a126b32e7e9ee3ada0ceef2c5b4f59

## Commits
05c94c2 fix(analytics): add 180d label to period comparison e4d26e2 feat(analytics): add 180d period for share and filters

## Stat
 src/entities/analytics/index.ts                    |  7 ++++-  .../analytics/lib/calculateActivityHeatmap.ts      |  1 +  .../analytics/lib/calculateExerciseRows.ts         |  1 +  .../analytics/lib/selectSessionsByPeriod.ts        | 34 +++++++++++++++-------  src/entities/analytics/model/types.ts              |  2 +-  src/features/analyticsFilters/model/types.ts       |  1 +  .../model/formatPeriodComparison.ts                |  1 +  .../ui/AnalyticsPeriodSegmentedControl.tsx         |  1 +  8 files changed, 35 insertions(+), 13 deletions(-)

## Diff
diff --git a/src/entities/analytics/index.ts b/src/entities/analytics/index.ts index d3c2524..2db3231 100644 --- a/src/entities/analytics/index.ts +++ b/src/entities/analytics/index.ts @@ -9,12 +9,17 @@ export type {    FrequencyMetrics,    MuscleBalance,    MuscleBalanceItem,    PeriodComparison,    TrendPoint,  } from "./model/types";    export { buildDashboardAnalytics } from "./lib/buildDashboardAnalytics";  export { calculateExerciseTonnageTrend } from "./lib/calculateTrends";  export { normalizeTrainingSessions } from "./lib/normalizeTrainingSessions"; -export { selectSessionsByPeriod } from "./lib/selectSessionsByPeriod"; +export { +  selectSessionsByPeriod, +  getPeriodDayCount, +  getPeriodDateRange, +  PERIOD_TO_DAYS, +} from "./lib/selectSessionsByPeriod";   diff --git a/src/entities/analytics/lib/calculateActivityHeatmap.ts b/src/entities/analytics/lib/calculateActivityHeatmap.ts index 9225293..5836b1c 100644 --- a/src/entities/analytics/lib/calculateActivityHeatmap.ts +++ b/src/entities/analytics/lib/calculateActivityHeatmap.ts @@ -1,18 +1,19 @@  import dayjs from "dayjs";  import type { ActivityHeatmap, AnalyticsPeriod, TrainingSessionStat } from "../model/types";  import { parseDateKey } from "./dateKey";    const PERIOD_TO_DAYS: Record<AnalyticsPeriod, number> = {    "7d": 7,    "30d": 30,    "90d": 90, +  "180d": 180,    "365d": 365,  };    const toIntensity = (sessions: number, maxSessions: number): 0 | 1 | 2 | 3 | 4 => {    if (sessions === 0 || maxSessions === 0) {      return 0;    }    const ratio = sessions / maxSessions;    if (ratio >= 0.8) {      return 4; diff --git a/src/entities/analytics/lib/calculateExerciseRows.ts b/src/entities/analytics/lib/calculateExerciseRows.ts index f14aef0..0390162 100644 --- a/src/entities/analytics/lib/calculateExerciseRows.ts +++ b/src/entities/analytics/lib/calculateExerciseRows.ts @@ -1,17 +1,18 @@  import dayjs from "dayjs";  import type { AnalyticsPeriod, ExerciseTrendRow, TrainingSessionStat } from "../model/types";    const PERIOD_TO_DAYS: Record<AnalyticsPeriod, number> = {    "7d": 7,    "30d": 30,    "90d": 90, +  "180d": 180,    "365d": 365,  };    const getTrendDatesByPeriod = (period: AnalyticsPeriod) => {    const periodInDays = PERIOD_TO_DAYS[period];    const endDate = dayjs().endOf("day");      return Array.from({ length: periodInDays }, (_, index) =>      endDate.subtract(periodInDays - 1 - index, "day").format("DD-MM-YYYY"),    ); diff --git a/src/entities/analytics/lib/selectSessionsByPeriod.ts b/src/entities/analytics/lib/selectSessionsByPeriod.ts index 4f50af0..be12f08 100644 --- a/src/entities/analytics/lib/selectSessionsByPeriod.ts +++ b/src/entities/analytics/lib/selectSessionsByPeriod.ts @@ -1,55 +1,67 @@ -import dayjs from "dayjs"; +import dayjs, { type Dayjs } from "dayjs";  import type { AnalyticsPeriod, TrainingSessionStat } from "../model/types";  import { parseDateKey } from "./dateKey";   -const PERIOD_TO_DAYS: Record<AnalyticsPeriod, number> = { +export const PERIOD_TO_DAYS: Record<AnalyticsPeriod, number> = {    "7d": 7,    "30d": 30,    "90d": 90, +  "180d": 180,    "365d": 365,  };   +export const getPeriodDayCount = (period: AnalyticsPeriod) => PERIOD_TO_DAYS[period]; + +export const getPeriodDateRange = ( +  period: AnalyticsPeriod, +  baseDate: Dayjs = dayjs(), +) => { +  const periodInDays = PERIOD_TO_DAYS[period]; +  const end = baseDate.endOf("day"); +  const start = end.subtract(periodInDays - 1, "day").startOf("day"); +  return { +    start, +    end, +    startDateKey: start.format("DD-MM-YYYY"), +    endDateKey: end.format("DD-MM-YYYY"), +  }; +}; +  const isInRange = (targetDateKey: string, startDate: dayjs.Dayjs, endDate: dayjs.Dayjs) => {    const targetDate = parseDateKey(targetDateKey);    if (!targetDate) {      return false;    }      return (      targetDate.valueOf() >= startDate.startOf("day").valueOf() &&      targetDate.valueOf() <= endDate.endOf("day").valueOf()    );  };    export const selectSessionsByPeriod = (    sessions: TrainingSessionStat[],    period: AnalyticsPeriod,    baseDate = dayjs(),  ) => { -  const periodInDays = PERIOD_TO_DAYS[period]; -  const endDate = baseDate.endOf("day"); -  const startDate = endDate.subtract(periodInDays - 1, "day").startOf("day"); +  const { start, end } = getPeriodDateRange(period, baseDate);   -  return sessions.filter((session) => isInRange(session.dateKey, startDate, endDate)); +  return sessions.filter((session) => isInRange(session.dateKey, start, end));  };    export const selectPreviousSessionsByPeriod = (    sessions: TrainingSessionStat[],    period: AnalyticsPeriod,    baseDate = dayjs(),  ) => { +  const { start: currentStartDate } = getPeriodDateRange(period, baseDate);    const periodInDays = PERIOD_TO_DAYS[period]; -  const currentStartDate = baseDate -    .endOf("day") -    .subtract(periodInDays - 1, "day") -    .startOf("day");    const previousEndDate = currentStartDate.subtract(1, "day").endOf("day");    const previousStartDate = previousEndDate      .subtract(periodInDays - 1, "day")      .startOf("day");      return sessions.filter((session) =>      isInRange(session.dateKey, previousStartDate, previousEndDate),    );  }; - diff --git a/src/entities/analytics/model/types.ts b/src/entities/analytics/model/types.ts index a2cdb24..1725067 100644 --- a/src/entities/analytics/model/types.ts +++ b/src/entities/analytics/model/types.ts @@ -1,11 +1,11 @@ -export type AnalyticsPeriod = "7d" | "30d" | "90d" | "365d"; +export type AnalyticsPeriod = "7d" | "30d" | "90d" | "180d" | "365d";    export interface DateRange {    startDateKey: string;    endDateKey: string;  }    export interface AnalyticsFilters {    period: AnalyticsPeriod;    exerciseId: string;    category: string; diff --git a/src/features/analyticsFilters/model/types.ts b/src/features/analyticsFilters/model/types.ts index 7184873..b9a487f 100644 --- a/src/features/analyticsFilters/model/types.ts +++ b/src/features/analyticsFilters/model/types.ts @@ -8,13 +8,14 @@ export interface AnalyticsPeriodOption {  export const DEFAULT_ANALYTICS_FILTERS: AnalyticsFilters = {    period: "30d",    exerciseId: "",    category: "",  };    export const ANALYTICS_PERIOD_OPTIONS: AnalyticsPeriodOption[] = [    { value: "7d", label: "7 ╨┤╨╜╨╡╨╣" },    { value: "30d", label: "30 ╨┤╨╜╨╡╨╣" },    { value: "90d", label: "90 ╨┤╨╜╨╡╨╣" }, +  { value: "180d", label: "180 ╨┤╨╜╨╡╨╣" },    { value: "365d", label: "1 ╨│╨╛╨┤" },  ];   diff --git a/src/features/analyticsPeriodCompare/model/formatPeriodComparison.ts b/src/features/analyticsPeriodCompare/model/formatPeriodComparison.ts index 1871b78..8370da4 100644 --- a/src/features/analyticsPeriodCompare/model/formatPeriodComparison.ts +++ b/src/features/analyticsPeriodCompare/model/formatPeriodComparison.ts @@ -1,17 +1,18 @@  import type { AnalyticsPeriod, PeriodComparison } from "@/entities/analytics";  import { formatTonnage } from "@/shared/lib";    const PERIOD_LABEL: Record<AnalyticsPeriod, string> = {    "7d": "7 ╨┤╨╜╨╡╨╣",    "30d": "30 ╨┤╨╜╨╡╨╣",    "90d": "90 ╨┤╨╜╨╡╨╣", +  "180d": "180 ╨┤╨╜╨╡╨╣",    "365d": "1 ╨│╨╛╨┤",  };    const formatDeltaSign = (value: number) => {    if (value > 0) {      return "+";    }    return "";  };   diff --git a/src/widgets/analyticsDashboard/ui/AnalyticsPeriodSegmentedControl.tsx b/src/widgets/analyticsDashboard/ui/AnalyticsPeriodSegmentedControl.tsx index ebbae05..4243c7a 100644 --- a/src/widgets/analyticsDashboard/ui/AnalyticsPeriodSegmentedControl.tsx +++ b/src/widgets/analyticsDashboard/ui/AnalyticsPeriodSegmentedControl.tsx @@ -1,17 +1,18 @@  import type { AnalyticsPeriod } from "@/entities/analytics";  import { cn } from "@/shared/ui/lib/utils";    const PERIOD_OPTIONS: Array<{ value: AnalyticsPeriod; label: string }> = [    { value: "7d", label: "7╨┤" },    { value: "30d", label: "30╨┤" },    { value: "90d", label: "90╨┤" }, +  { value: "180d", label: "180╨┤" },    { value: "365d", label: "1╨│" },  ];    interface AnalyticsPeriodSegmentedControlProps {    value: AnalyticsPeriod;    onChange: (period: AnalyticsPeriod) => void;  }    export const AnalyticsPeriodSegmentedControl = ({    value,
