/** MET (метаболические эквиваленты) для fallback по типу движения. */
export const MET_BY_EXERCISE_KEY: Record<string, number> = {
  squats: 5.0,
  leg_press: 3.5,
  lunges: 4.0,
  overhead_press: 4.5,
  lateral_raises: 3.0,
  default: 4.0,
};
