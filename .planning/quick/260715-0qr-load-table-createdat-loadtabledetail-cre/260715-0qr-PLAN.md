---
phase: 260715-0qr-load-table-createdat-loadtabledetail-cre
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/entities/loadTable/lib/getLoadTableCurrentWeek.ts
  - src/entities/loadTable/lib/getPlanSetsForWeek.ts
  - src/entities/loadTable/slice/loadTableStore.ts
  - src/entities/loadTable/index.ts
  - src/entities/calendarDay/slice/calendarStore.ts
  - src/features/loadTable/ui/LoadTableWeekGrid.tsx
  - src/features/loadTable/ui/LoadTableDetail.tsx
autonomous: true
requirements:
  - QUICK-0QR-01
must_haves:
  truths:
    - "Current week is derived as min(16, floor(sessionCount/2)+1) counting only logged sessions for that catalogExerciseId on or after load-table createdAt."
    - "LoadTableWeekGrid visually highlights the currentWeek row with theme tokens."
    - "Отслеживать applies three identical plan sets (weight/reps for current week) to the matching exercise on the selected calendar day, or shows a clear Russian message if missing."
    - "Сбросить opens a confirm dialog; on confirm resetExerciseProgress sets createdAt to now so week returns to 1."
  artifacts:
    - path: src/entities/loadTable/lib/getLoadTableCurrentWeek.ts
      provides: "Async sessionCount → currentWeek since createdAt"
    - path: src/entities/loadTable/lib/getPlanSetsForWeek.ts
      provides: "Three plan sets {weight, reps} for a week × maxKg"
    - path: src/features/loadTable/ui/LoadTableDetail.tsx
      provides: "Отслеживать / Сбросить + confirm dialog + status message"
  key_links:
    - from: "getLoadTableCurrentWeek(catalogExerciseId, createdAt)"
      to: "readWorkoutMonthBucketsForKeys / listWorkoutMonthKeys"
      via: "session days with reps>0 or weight>0 (D-01)"
    - from: "Отслеживать"
      to: "calendarStore selected day exercise by catalogExerciseId"
      via: "sync to exactly 3 plan sets (D-02)"
    - from: "Сбросить confirm"
      to: "loadTableStore.resetExerciseProgress(id)"
      via: "createdAt = new ISO timestamp (D-04)"
---

<objective>
Автозаполнение подходов/веса из load-table по счёту сессий с `createdAt`; в `LoadTableDetail` — подсветка текущей недели, «Отслеживать» (префилл в дневник) и «Сбросить» с confirm-модалкой.

Purpose: Новичок видит текущую неделю программы и одним нажатием подставляет плановые 3×вес/reps в выбранный день.
Output: week/session helpers, store reset, grid highlight, detail Track/Reset UX wired to calendar.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/quick/260715-0qr-load-table-createdat-loadtabledetail-cre/260715-0qr-CONTEXT.md
@src/entities/loadTable/model/schedule.ts
@src/entities/loadTable/lib/buildWeekRows.ts
@src/entities/loadTable/lib/roundToHalfKg.ts
@src/entities/loadTable/slice/loadTableStore.ts
@src/features/loadTable/ui/LoadTableDetail.tsx
@src/features/loadTable/ui/LoadTableWeekGrid.tsx
@src/shared/lib/findLastExerciseSession.ts
@src/shared/lib/storage.ts
@src/entities/calendarDay/slice/calendarStore.ts
@src/features/fullExerciseList/ui/DeleteDialog.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Session-week helpers and resetExerciseProgress</name>
  <files>src/entities/loadTable/lib/getLoadTableCurrentWeek.ts, src/entities/loadTable/lib/getPlanSetsForWeek.ts, src/entities/loadTable/slice/loadTableStore.ts, src/entities/loadTable/index.ts</files>
  <action>
Implement pure domain helpers and store reset per locked CONTEXT decisions D-01 and D-04 (store half).

1. `getLoadTableCurrentWeek(catalogExerciseId, createdAtIso): Promise&lt;{ sessionCount: number; currentWeek: number }&gt;`
   - Reuse journal storage patterns next to `findLastExerciseSession` / month buckets: `listWorkoutMonthKeys` + `readWorkoutMonthBucketsForKeys` (or equivalent from `src/shared/lib/storage.ts` / storageAdapter).
   - Scan days from `createdAt` start-of-day inclusive through today.
   - A session day counts if any exercise with that `catalogExerciseId` has at least one set with `reps &gt; 0` or `weight &gt; 0` (D-01).
   - Formula (D-01): `currentWeek = min(16, Math.floor(sessionCount / 2) + 1)`. Zero sessions → week 1.
   - Do not introduce a persisted `currentWeek` field.

2. `getPlanSetsForWeek(maxKg, week): Array&lt;{ weight: number; reps: number }&gt;`
   - Resolve schedule row for `week` (clamp 1–16); weight via existing `roundToHalfKg(maxKg * percent / 100)`; reps from schedule; always return **exactly 3** identical `{ weight, reps }` objects (D-01).

3. Store action `resetExerciseProgress(id)` (D-04): set that exercise `createdAt` to `new Date().toISOString()`, clear error state; no other fields change.

4. Export new helpers and ensure store action is available via `src/entities/loadTable/index.ts`.
  </action>
  <verify>
    <automated>pnpm exec tsc --noEmit -p tsconfig.app.json 2>&amp;1 | Select-Object -First 40; rg -n "getLoadTableCurrentWeek|getPlanSetsForWeek|resetExerciseProgress" src/entities/loadTable</automated>
  </verify>
  <done>
Helpers return week from post-createdAt session math; store can reset progress via createdAt; barrel exports updated.
  </done>
</task>

<task type="auto">
  <name>Task 2: Highlight current week and calendar set sync</name>
  <files>src/features/loadTable/ui/LoadTableWeekGrid.tsx, src/entities/calendarDay/slice/calendarStore.ts</files>
  <action>
1. `LoadTableWeekGrid` (D-03): accept optional/required `currentWeek: number`. On the matching row apply token highlight (`bg-muted` or `bg-accent` / `bg-accent/40` — theme tokens only, no raw hex, no CSS modules, className literals inline). Other rows unchanged.

2. Calendar store (D-02 wiring support, Claude discretion — predictable beginner UX): add a focused action e.g. `syncExerciseSetsFromPlan(exercise, sets: Array&lt;{ weight: number; reps: number }&gt;)` that, for the **selectedDate** day card matching `exercise.id`, replaces/syncs sets to exactly the provided plan list (length 3): overwrite weight/reps on existing sets, add missing sets (reuse timing helpers from `addSetToExercise` / calorie defaults if needed), remove excess sets. Persist via existing `replaceExercises` / `saveDaysToLocalStorage` path. Do not silently create a new exercise card if missing — caller handles message.
  </action>
  <verify>
    <automated>pnpm exec tsc --noEmit -p tsconfig.app.json 2>&amp;1 | Select-Object -First 40; rg -n "currentWeek|syncExerciseSetsFromPlan" src/features/loadTable/ui/LoadTableWeekGrid.tsx src/entities/calendarDay/slice/calendarStore.ts</automated>
  </verify>
  <done>
Week grid highlights currentWeek; calendar can sync an exercise to three plan sets on the selected day.
  </done>
</task>

<task type="auto">
  <name>Task 3: LoadTableDetail Track / Reset UX</name>
  <files>src/features/loadTable/ui/LoadTableDetail.tsx</files>
  <action>
Wire detail UI per D-02, D-03, D-04. Russian copy at discretion (clear for beginners).

1. On mount / when `exercise.id` or `createdAt` changes: load `currentWeek` via `getLoadTableCurrentWeek(exercise.catalogExerciseId, exercise.createdAt)` and pass into `LoadTableWeekGrid` (D-03).

2. Top actions above the grid (D-02 / D-04): buttons «Отслеживать» and «Сбросить» (outline/secondary variants via existing Button).

3. «Отслеживать» (D-02):
   - Compute plan sets with `getPlanSetsForWeek(exercise.maxKg, currentWeek)`.
   - Find exercise on `useCalendarStore` selected day whose `catalogExerciseId` matches; if none — show inline status text (no toast library in project), e.g. that the exercise is not in the selected day; do not throw.
   - If found — call `syncExerciseSetsFromPlan` so the card shows three plan weight/reps immediately. Load-table values win over last-session prefill for this action (D-02). Optionally show brief success text.

4. «Сбросить» (D-04): open confirm Dialog (same shadcn Dialog pattern as `DeleteDialog` — title/description/Отмена/confirm). Warn that the week counter will reset. On confirm call `resetExerciseProgress(exercise.id)`, close dialog, recompute `currentWeek` (should become 1).

5. Styling: Tailwind tokens only; keep existing MAX / body-weight fields; do not add CSS modules.
  </action>
  <verify>
    <automated>pnpm exec tsc --noEmit -p tsconfig.app.json 2>&amp;1 | Select-Object -First 40; rg -n "Отслеживать|Сбросить|getLoadTableCurrentWeek|syncExerciseSetsFromPlan|resetExerciseProgress" src/features/loadTable/ui/LoadTableDetail.tsx</automated>
  </verify>
  <done>
Detail shows highlighted week, Track applies plan sets to selected-day exercise or explains absence, Reset confirm updates createdAt and returns week to 1.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| localStorage / app storage → UI | Workout journal and load-table persist are trusted local client data |
| LoadTableDetail → calendarStore | User-triggered mutation of selected-day sets |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-0qr-01 | Tampering | getLoadTableCurrentWeek journal scan | low | accept | Local-first v1; malformed days already skipped by type guards like findLastExerciseSession |
| T-0qr-02 | Elevation of Privilege | syncExerciseSetsFromPlan | low | mitigate | Only mutate exercise already present on selected day; never invent catalog cards from Track |
| T-0qr-03 | Denial of Service | unbounded month scan | medium | mitigate | Limit scan to month keys from createdAt through today (not entire device storage forever beyond that window) |
| T-0qr-SC | Tampering | npm/pip/cargo installs | low | accept | No new packages in this plan |
</threat_model>

<verification>
- `pnpm exec tsc --noEmit -p tsconfig.app.json` passes.
- Manual smoke: add load-table exercise, log 0/2/3 sessions with weight or reps, confirm week 1 / 2 / 2; highlight moves; Track fills 3 sets on home selected day; Reset confirm restores week 1.
</verification>

<success_criteria>
- D-01 week math implemented without a stored currentWeek field.
- D-02 Track button prefills selected-day exercise or shows a clear message.
- D-03 current week row highlighted with tokens.
- D-04 Reset confirm updates createdAt via store action.
</success_criteria>

<source_audit>
SOURCE    | ID      | Feature/Requirement                                      | Plan | Status  | Notes
--------- | ------- | -------------------------------------------------------- | ---- | ------- | -----
GOAL      | —       | Prefill + week UI from load-table session count           | 01   | COVERED |
REQ       | QUICK-0QR-01 | Quick task scope from description                    | 01   | COVERED |
RESEARCH  | —       | (none — quick mode, no research phase)                   | —    | n/a     |
CONTEXT   | D-01    | Week = min(16, floor(sessions/2)+1) since createdAt        | 01   | COVERED | Task 1
CONTEXT   | D-02    | Отслеживать → prefill selected calendar day              | 01   | COVERED | Tasks 2–3
CONTEXT   | D-03    | Highlight currentWeek in grid                            | 01   | COVERED | Tasks 2–3
CONTEXT   | D-04    | Сбросить + modal → reset createdAt                       | 01   | COVERED | Tasks 1, 3
</source_audit>

<output>
Create `.planning/quick/260715-0qr-load-table-createdat-loadtabledetail-cre/260715-0qr-SUMMARY.md` when done
</output>
