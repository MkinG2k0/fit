---
phase: 260715-052-16-xlsx-max-body-metrics-editable
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/entities/loadTable/model/types.ts
  - src/entities/loadTable/model/schedule.ts
  - src/entities/loadTable/lib/roundToHalfKg.ts
  - src/entities/loadTable/lib/oneRepMaxFormulas.ts
  - src/entities/loadTable/lib/buildWeekRows.ts
  - src/entities/loadTable/api/loadTableApi.ts
  - src/entities/loadTable/slice/loadTableStore.ts
  - src/entities/loadTable/index.ts
  - src/features/loadTable/ui/LoadTableList.tsx
  - src/features/loadTable/ui/AddLoadTableExerciseDialog.tsx
  - src/features/loadTable/ui/LoadTableDetail.tsx
  - src/features/loadTable/ui/LoadTableWeekGrid.tsx
  - src/features/loadTable/index.ts
  - src/pages/LoadTablePage/ui/LoadTablePage.tsx
  - src/pages/LoadTablePage/index.ts
  - src/pages/index.ts
  - src/app/router/routes.tsx
  - src/app/layout/AppLayout.tsx
  - src/features/profileDropDownMenu/ui/profileDropDownMenu.tsx
autonomous: true
requirements:
  - QUICK-LOAD-TABLE-01
must_haves:
  truths:
    - "Profile menu opens /load-table; header title Таблица нагрузок."
    - "User can add catalog exercises; each entry stores catalogExerciseId, maxKg (MAX), maxReps; list shows them."
    - "On first add confirm, a body-metrics entry with weightKg is created via addEntry; detail shows latest body weight read-only."
    - "Detail shows fixed 16-week rows with % / kg / reps and Brzycki / Epley / Lander / avg from week working weight × week reps; kg = round_half(MAX × %/100)."
    - "maxReps and MAX are editable on detail; caption 3 подхода / 2 раза в неделю is UI-only."
  artifacts:
    - path: src/entities/loadTable/model/schedule.ts
      provides: "Fixed 16-week percent/reps template from Жим.xlsx"
    - path: src/entities/loadTable/lib/oneRepMaxFormulas.ts
      provides: "Brzycki / Epley / Lander + avg from working weight and week reps"
    - path: src/entities/loadTable/slice/loadTableStore.ts
      provides: "Persisted list of load-table exercises with MAX and maxReps"
    - path: src/pages/LoadTablePage/ui/LoadTablePage.tsx
      provides: "List ↔ detail page on /load-table"
  key_links:
    - from: "AddLoadTableExerciseDialog confirm"
      to: "loadTableStore.addExercise + bodyMetricsStore.addEntry(weightKg)"
      via: "first-add flow (D-01)"
    - from: "LoadTableWeekGrid"
      to: "buildWeekRows(maxKg, LOAD_TABLE_SCHEDULE)"
      via: "week kg and 1RM formula columns (D-02, D-03)"
    - from: "ProfileDropDownMenu / AppRoutes / PAGE_TITLES"
      to: "/load-table"
      via: "nav + lazy page (D-05)"
---

<objective>
Добавить фичу «Таблица нагрузок»: список упражнений каталога с пользовательским MAX, editable maxReps, вес тела из body-metrics и фиксированная 16-недельная таблица как в Жим.xlsx (per D-01…D-06 из Q1–Q6).

Purpose: Новичок видит программные %×MAX, рабочие кг и оценки 1ПМ по неделям без ручного Excel.
Output: entity store + формулы/расписание + feature UI + page/route/nav.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/quick/260715-052-16-xlsx-max-body-metrics-editable/260715-052-CONTEXT.md
@src/entities/bodyMetrics/slice/bodyMetricsStore.ts
@src/entities/bodyMetrics/model/types.ts
@src/entities/bodyMetrics/model/selectors.ts
@src/entities/bodyMetrics/api/bodyMetricsApi.ts
@src/entities/exercise/slice/exerciseStore.ts
@src/features/exercise/ui/ExerciseNameSelector.tsx
@src/pages/BodyMetricsPage/ui/BodyMetricsPage.tsx
@src/app/router/routes.tsx
@src/app/layout/AppLayout.tsx
@src/features/profileDropDownMenu/ui/profileDropDownMenu.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Domain schedule, 1RM helpers, loadTable store</name>
  <files>src/entities/loadTable/model/types.ts, src/entities/loadTable/model/schedule.ts, src/entities/loadTable/lib/roundToHalfKg.ts, src/entities/loadTable/lib/oneRepMaxFormulas.ts, src/entities/loadTable/lib/buildWeekRows.ts, src/entities/loadTable/api/loadTableApi.ts, src/entities/loadTable/slice/loadTableStore.ts, src/entities/loadTable/index.ts</files>
  <action>
Create `src/entities/loadTable/` (FSD entity, local-first like body-metrics).

1. Types (D-06): `LoadTableExercise` with `id`, `catalogExerciseId`, `maxKg` (user MAX), `maxReps`, `createdAt`. Persist only these — not body weight.

2. Schedule (D-03): export `LOAD_TABLE_SCHEDULE` exactly: week 1–16 with percent/reps pairs from CONTEXT specifics (75/5, 78/5, 82/5, 85/5, 88/3, 92/3, 95/2, 98/2, 82/5, 85/5, 88/5, 92/5, 95/3, 98/3, 102/2, 105/2). Do not invent other weeks.

3. Pure libs:
   - `roundToHalfKg(n)` — Excel-style half-kg steps (e.g. 37.5, 39.0).
   - `oneRepMaxFormulas(w, r)` — Brzycki `w*36/(37-r)`, Epley `w*(1+r/30)`, Lander `(100*w)/(101.3-2.67123*r)`, `avg` mean of three. Guard r &lt; 37 for Brzycki denominator; if invalid, return nulls rather than Infinity.
   - `buildWeekRows(maxKg)` — for each schedule row: `weightKg = roundToHalfKg(maxKg * percent / 100)`, then formula inputs are **week working weight + week reps** (not body weight; D-01/D-03).

4. Store: Zustand + `persist` + `createJSONStorage(() =&gt; zustandAppStorage)` (same adapter as exercise/body-metrics). Storage key e.g. `load-table-store` via thin `api/loadTableApi.ts`. Actions: `addExercise({ catalogExerciseId, maxKg, maxReps })`, `updateExercise(id, { maxKg?, maxReps? })`, `removeExercise(id)`. Reject duplicate `catalogExerciseId` or no-op with clear status (discretion: skip silently or set errorMessage — prefer errorMessage).

5. Barrel `index.ts` exporting types, schedule, helpers, store.
  </action>
  <verify>
    <automated>pnpm exec tsc --noEmit -p tsconfig.app.json 2>&amp;1 | head -n 40; rg -n "LOAD_TABLE_SCHEDULE|buildWeekRows|oneRepMaxFormulas|useLoadTableStore" src/entities/loadTable</automated>
  </verify>
  <done>
Entity exposes fixed 16-week schedule, half-kg rounding, 1RM trio+avg from working load, and a persisted store with add/update/remove keyed by catalogExerciseId.
  </done>
</task>

<task type="auto">
  <name>Task 2: Feature UI — list, add dialog, detail table</name>
  <files>src/features/loadTable/ui/LoadTableList.tsx, src/features/loadTable/ui/AddLoadTableExerciseDialog.tsx, src/features/loadTable/ui/LoadTableDetail.tsx, src/features/loadTable/ui/LoadTableWeekGrid.tsx, src/features/loadTable/index.ts</files>
  <action>
Build feature UI on Tailwind tokens only (inline className; no CSS modules; no raw hex). Reuse shadcn Dialog/Button/Input and catalog picker patterns (ExerciseNameSelector or Command from fullExerciseList).

1. `LoadTableList` (D-06): render entries from `useLoadTableStore`; resolve names via `findCatalogExerciseById` / exercise store. Empty state + «Добавить» opens dialog. Row click selects detail (callback `onSelect(id)`). Optional remove control.

2. `AddLoadTableExerciseDialog` (D-01, D-02, D-06):
   - Fields: catalog exercise picker; MAX (`maxKg`); max reps; body weight (`weightKg`).
   - Prefill body weight from latest body-metrics: `selectSortedBodyMetricsEntries(entries)` then first entry with finite `measurements.weightKg` (same sort as body-metrics selectors).
   - On confirm: call `bodyMetricsStore.addEntry` with `recordedAt` = today ISO date string and `measurements: { weightKg }` (always write a new entry on confirm per D-01 / discretion); then `loadTableStore.addExercise` with catalog id, maxKg, maxReps. Validate positive finite numbers before submit.

3. `LoadTableDetail` (D-01, D-02, D-04, D-06):
   - Show exercise name; editable MAX and editable maxReps (persist via `updateExercise`).
   - Body weight: read-only from latest body-metrics `weightKg` (display only — never mutate body-metrics from detail).
   - Static caption: «3 подхода / 2 раза в неделю» (UI hint only; D-04).
   - Do **not** add a «Т. Макс» block computed from body weight × maxReps (discretion: omit).

4. `LoadTableWeekGrid` (D-02, D-03): table columns Неделя | вес % | вес кг | раз | Ф. Бржыки | Ф. Эйпли | Ф. Лэндера | avg. Data from `buildWeekRows(maxKg)`. Horizontal scroll on narrow screens (`overflow-x-auto`). Format kg/1RM to one decimal where useful.

5. Export public API from `features/loadTable/index.ts`.
  </action>
  <verify>
    <automated>pnpm exec tsc --noEmit -p tsconfig.app.json 2>&amp;1 | head -n 40; rg -n "LoadTableList|AddLoadTableExerciseDialog|LoadTableDetail|LoadTableWeekGrid|3 подхода" src/features/loadTable</automated>
  </verify>
  <done>
List/add/detail UI works: add writes body-metrics weightKg; detail shows read-only body weight, editable MAX/maxReps, fixed 16-week grid with formulas from week load.
  </done>
</task>

<task type="auto">
  <name>Task 3: Page, route, PAGE_TITLES, profile menu</name>
  <files>src/pages/LoadTablePage/ui/LoadTablePage.tsx, src/pages/LoadTablePage/index.ts, src/pages/index.ts, src/app/router/routes.tsx, src/app/layout/AppLayout.tsx, src/features/profileDropDownMenu/ui/profileDropDownMenu.tsx</files>
  <action>
Wire navigation (D-05). Discretion slug/label: route `/load-table`, menu «Таблица нагрузок» (lucide `Table2` or `ChartColumn` adjacent to analytics / body-metrics).

1. `LoadTablePage`: local state `selectedId: string | null`. If null → `LoadTableList`; else → `LoadTableDetail` with back that clears selection (keeps single pathname for AppLayout title match). Thin page wrapper like BodyMetricsPage.

2. `pages/LoadTablePage/index.ts` + export from `pages/index.ts`.

3. `routes.tsx`: lazy import `LoadTablePage`; ProtectedRoute at `/load-table` (same pattern as `/body-metrics`).

4. `AppLayout` PAGE_TITLES: `"/load-table": "Таблица нагрузок"`.

5. `profileDropDownMenu.tsx`: menu button near analytics / параметры тела → navigate `/load-table`; close popover on navigate.
  </action>
  <verify>
    <automated>pnpm exec tsc --noEmit -p tsconfig.app.json 2>&amp;1 | head -n 40; rg -n "load-table|LoadTablePage|Таблица нагрузок" src/app src/pages src/features/profileDropDownMenu</automated>
  </verify>
  <done>
`/load-table` is reachable from profile menu, shows titled page, and hosts list/detail of the load-table feature.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI form → loadTable / bodyMetrics stores | Untrusted numeric inputs (MAX, maxReps, weightKg) enter persisted local state |
| localStorage / Capacitor prefs | Client-only persistence; no server trust boundary in v1 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-260715-052-01 | Tampering | Add/detail number fields | low | mitigate | Validate finite positive numbers before addEntry / addExercise / updateExercise |
| T-260715-052-02 | Elevation of Privilege | N/A (local-only) | low | accept | No auth boundary change; ProtectedRoute unchanged |
| T-260715-052-03 | Information Disclosure | Persisted stores | low | accept | Same local-first model as body-metrics; no new network egress |
| T-260715-052-04 | Denial of Service | oneRepMaxFormulas | medium | mitigate | Guard Brzycki denominator (r &lt; 37); avoid Infinity/NaN in grid cells |
| T-260715-052-SC | Tampering | package installs | low | accept | No new npm/pnpm packages in this plan |
</threat_model>

<verification>
- `pnpm exec tsc --noEmit -p tsconfig.app.json` exits 0 after all tasks.
- Schedule length is 16; week-1 row: 75%, 5 reps; week-16: 105%, 2 reps.
- Manual smoke: open menu → Таблица нагрузок → add exercise with weight → detail shows body weight read-only, editable MAX/maxReps, 16 rows with kg and three formulas + avg.
</verification>

<success_criteria>
- All locked decisions D-01…D-06 (Q1–Q6) implemented without deferred-scope leakage.
- Persistence survives reload via Zustand persist.
- Route `/load-table` + PAGE_TITLES + profile entry present.
- Week kg and 1RM columns derive from user MAX × schedule, not from body weight.
</success_criteria>

<output>
Create `.planning/quick/260715-052-16-xlsx-max-body-metrics-editable/260715-052-SUMMARY.md` when done
</output>

## Source Audit

| SOURCE | ID | Item | Plan | Status |
|--------|-----|------|------|--------|
| GOAL | — | Таблица нагрузок: 16 недель как Жим.xlsx, MAX, body-metrics, editable maxReps, список, роут | 01 | COVERED |
| REQ | QUICK-LOAD-TABLE-01 | Quick feature delivery | 01 | COVERED |
| RESEARCH | — | (none; quick, no research) | — | N/A |
| CONTEXT | D-01 (Q1) | Body weight from/to body-metrics; detail read-only | 01 T1–T2 | COVERED |
| CONTEXT | D-02 (Q2) | User MAX; week kg = MAX×%/100 half-kg | 01 T1–T2 | COVERED |
| CONTEXT | D-03 (Q3) | Fixed 16-week template + 1RM from week load | 01 T1–T2 | COVERED |
| CONTEXT | D-04 (Q4) | Caption 3×2/week UI-only | 01 T2 | COVERED |
| CONTEXT | D-05 (Q5) | New route + menu + PAGE_TITLES | 01 T3 | COVERED |
| CONTEXT | D-06 (Q6) | Multi-exercise list; maxReps/MAX per entry | 01 T1–T2 | COVERED |
