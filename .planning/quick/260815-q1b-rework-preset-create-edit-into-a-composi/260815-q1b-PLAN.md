---
phase: q1b-rework-preset-create-edit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/createPreset/ui/CreatePreset.tsx
  - src/features/createPreset/ui/AddPresetExercisesDrawer.tsx
  - src/features/createPreset/ui/PresetCompositionList.tsx
  - src/features/createPreset/lib/presetExerciseIds.ts
autonomous: true
requirements:
  - Q1B-01
  - Q1B-02
  - Q1B-03
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Create and edit preset screens show a name field plus a numbered ordered list of selected exercises, not a catalog of category checkboxes (D-01, D-02, D-07)"
    - "User adds exercises through a right-side catalog drawer using FullExerciseCommand, same pattern as home AddExercise (D-03)"
    - "User reorders the composition with motion Reorder (Reorder.Group / Reorder.Item / dragControls) like ExerciseList (D-04)"
    - "Routes /presets/create and /presets/edit?id= still work; create-from-workout still hydrates via location.state.initialExercises (D-05, D-06)"
    - "Saved presets still persist exercises as unique catalog id string[] through createTrainingPreset / updateTrainingPreset (existing contract)"
  artifacts:
    - src/features/createPreset/ui/CreatePreset.tsx
    - src/features/createPreset/ui/AddPresetExercisesDrawer.tsx
    - src/features/createPreset/ui/PresetCompositionList.tsx
    - src/features/createPreset/lib/presetExerciseIds.ts
  key_links:
    - "CreatePresetPage -> CreatePreset (editingPreset, initialExercises, onCancel) unchanged"
    - "CreatePreset.newPreset.exercises <-> PresetCompositionList Reorder values (string[] catalog ids)"
    - "AddPresetExercisesDrawer -> FullExerciseCommand variant exercises + useExerciseSelection -> appendUniqueExerciseIds"
    - "Save -> createTrainingPreset / updateTrainingPreset with NewPreset { presetName, exercises: string[] }"
---

<objective>
Rework preset create/edit into a composition-first page: the page is the preset (name + numbered ordered exercise list). Add via the existing catalog drawer picker; reorder with the home workout Reorder pattern. Catalog checkboxes are no longer the main editor (D-01, D-02, D-03, D-04).

Purpose: Beginners can see and edit preset order the same way they see a workout, instead of hunting through collapsed category checkboxes.

Output: CreatePreset rewritten around composition list + add drawer; helper for unique id append; Reorder list; routes and store contract unchanged (D-05, D-06).
</objective>

<execution_context>
@C:/Users/mk/.cursor/gsd-core/workflows/execute-plan.md
@C:/Users/mk/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@src/features/createPreset/ui/CreatePreset.tsx
@src/pages/CreatePresetPage/ui/CreatePresetPage.tsx
@src/features/addExercise/ui/AddExercise.tsx
@src/features/addExercise/lib/useExerciseSelection.ts
@src/features/addExercise/lib/useDrawerViewportStyle.ts
@src/widgets/exerciseList/ui/ExerciseList.tsx
@src/features/fullExerciseList/ui/fullExerciseCommand.tsx
@src/shared/ui/fixedBottomBar/FixedBottomBar.tsx
@src/entities/exercise/lib/catalogNameIndex.ts
@src/features/createPreset/model/types.ts
@src/app/layout/AppLayout.tsx
@src/app/router/routes.tsx
</context>

<tasks>

<task type="tracer">
  <name>End-to-end composition editor with catalog add drawer</name>
  <files>src/features/createPreset/lib/presetExerciseIds.ts, src/features/createPreset/ui/AddPresetExercisesDrawer.tsx, src/features/createPreset/ui/CreatePreset.tsx</files>
  <read_first>src/features/createPreset/ui/CreatePreset.tsx, src/features/addExercise/ui/AddExercise.tsx, src/features/addExercise/lib/useExerciseSelection.ts, src/features/addExercise/lib/useDrawerViewportStyle.ts, src/features/fullExerciseList/ui/fullExerciseCommand.tsx, src/pages/CreatePresetPage/ui/CreatePresetPage.tsx, src/entities/exercise/lib/catalogNameIndex.ts, src/shared/ui/fixedBottomBar/FixedBottomBar.tsx, src/features/createPreset/model/types.ts</read_first>
  <action>
Keep NewPreset.exercises as string[] catalog ids. Do not change TrainingPreset, exerciseStore create/update, routes.tsx, or CreatePresetPage props (D-05, D-06). Do not import or render AddExercise (it writes to the calendar and offers nested preset-create actions).

Add src/features/createPreset/lib/presetExerciseIds.ts with two named functions: dedupePreserveOrder(ids: string[]) returns unique ids in first-seen order, skipping blank/whitespace entries; appendUniqueExerciseIds(existing: string[], incoming: string[]) returns existing order unchanged then appends incoming ids not already present (also skipping blanks). Hydrate createInitialPreset through dedupePreserveOrder so Reorder values stay unique.

Add src/features/createPreset/ui/AddPresetExercisesDrawer.tsx copying the AddExercise drawer mechanics per D-03: Drawer direction right; open state driven by search param add-exercise=1 (same param name and set/delete/replace behavior as AddExercise, including visualViewport height via useDrawerViewportStyle); trigger Button label Добавить упражнение; title Добавьте упражнения; footer Добавить + Отмена. Inside the drawer render FullExerciseCommand with variant exercises, checkable checkbox, deletable false, scrollBottomPadding false. Use useExerciseSelection; pass selectedPresetCheckboxes as empty array and presetSelectHandler as a no-op so presets never appear and never merge. On Добавить: call onAdd(selectedExerciseCheckboxes), reset selection, close drawer. Do not include CreateButtons, CreateCategory, or navigate-to-create-exercise.

Rewrite CreatePreset.tsx per D-01, D-02, D-07, D-08: the page body is the preset. Keep name Input, duplicate-name error, create/update/save validation (name non-empty and exercises.length greater than 0), and handleClose via onCancel. Remove the inline catalog tree from this file (search field, category expand/collapse, and catalog checkbox rows). After hydrate, show a numbered list of newPreset.exercises (1-based). Resolve visible names with useCatalogNameById: nameById.get(id) or the fallback Упражнение недоступно for missing catalog entries. Each row has a remove control that filters that id out of newPreset.exercises (removal is list-only; the drawer only appends). Empty list: short Russian hint to use Добавить упражнение.

Place chrome like the home workout: scrollable list with bottom padding for the bar; FixedBottomBar contains AddPresetExercisesDrawer plus the primary save Button (Создать / Сохранить). Header back already cancels to /exercises — do not duplicate a second cancel row. On drawer submit, setExercises via appendUniqueExerciseIds. Keep className Tailwind token literals inline on JSX (D-08); do not add CSS module files. Use pnpm for any commands (D-09).
  </action>
  <verify>
    <automated>pnpm exec tsc -b --pretty false</automated>
  </verify>
  <done>
Create/edit page is a named composition list with add-via-drawer; saving still writes string[] ids; /presets/create, /presets/edit?id=, and location.state.initialExercises still hydrate; FullExerciseCommand exists only in AddPresetExercisesDrawer; CreatePreset.tsx no longer contains handleCategoryToggle or handleExerciseToggle; no new packages.
  </done>
</task>

<task type="auto">
  <name>Drag-reorder composition with ExerciseList Reorder pattern</name>
  <files>src/features/createPreset/ui/PresetCompositionList.tsx, src/features/createPreset/ui/CreatePreset.tsx</files>
  <read_first>src/widgets/exerciseList/ui/ExerciseList.tsx, src/features/createPreset/ui/CreatePreset.tsx, src/features/exercise/ui/ExerciseCard.tsx</read_first>
  <precondition>Task 1 committed: CreatePreset renders a numbered composition list of catalog ids and AddPresetExercisesDrawer appends unique ids.</precondition>
  <action>
Extract the numbered composition rows into src/features/createPreset/ui/PresetCompositionList.tsx and wire it from CreatePreset, passing exercises: string[], nameById, onReorder(ids: string[]), onRemove(id: string) per D-04.

Copy the home list Reorder mechanics from ExerciseList.tsx (not ExerciseCard internals): motion/react AnimatePresence, Reorder.Group as div, axis y, values={exercises}, onReorder={onReorder}, className flex flex-col gap-2; each row is Reorder.Item as div with value={id}, dragListener={false}, dragControls from useDragControls, same listItemMotionProps duration/ease as ExerciseList. Pointer down on a grip handle (lucide GripVertical) calls preventDefault then dragControls.start; do not attach drag to the remove control. Keep 1-based index visible on every row; indices follow array order after reorder.

Row chrome per D-07/D-08: compact card using bg-card text-card-foreground border-border rounded-xl; layout grip, number, truncated name, remove. touch-none cursor-grab on the handle only. CreatePreset onReorder must write the new id array onto newPreset.exercises with no catalog re-sort. Do not persist until the existing save Button. Do not change store shape. Do not add CSS module files.
  </action>
  <verify>
    <automated>pnpm exec tsc -b --pretty false</automated>
  </verify>
  <done>
Dragging a grip handle updates visible order and the string[] that save will persist; numbers follow the new order; remove still works; motion Reorder.Group/Item live in PresetCompositionList.tsx; CreatePresetPage and routes.tsx untouched.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User → preset editor | Preset name and catalog exercise ids chosen in the browser |
| Editor → zustand/localStorage | Saved TrainingPreset { presetName, exercises: string[] } persists locally |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-q1b-01 | Tampering | CreatePreset save path | low | accept | Local-first owner data; user can only mutate their own persisted presets. Keep writing catalog ids resolved by existing store create/update. |
| T-q1b-02 | Tampering | AddPresetExercisesDrawer | medium | mitigate | Accept exercise ids only from FullExerciseCommand catalog rows (no free-typed ids). appendUniqueExerciseIds skips blanks and duplicates. |
| T-q1b-03 | Information Disclosure | Preset name / list | low | accept | Names stay in localStorage as today; no new network or account sync in this task. |
| T-q1b-04 | Denial of Service | Reorder.Group | low | accept | List size bounded by catalog; Reorder operates on in-memory string[]. |
| T-q1b-05 | Elevation of Privilege | /presets/create|/presets/edit | low | accept | App remains local-first with no authz boundary on these routes. |
| T-q1b-06 | XSS (Injection) | Name field and row labels | medium | mitigate | Render presetName and catalog names as React text children / Input value only. Do not use HTML string injection APIs on this page. |
| T-q1b-SC | Tampering | package installs | high | accept | No new npm/pnpm packages. Reuse motion/react, lucide-react, and existing Drawer/Command already in the app. |
</threat_model>

<verification>
Run `pnpm exec tsc -b --pretty false`. Confirm:
- `rg FullExerciseCommand src/features/createPreset` matches AddPresetExercisesDrawer.tsx only
- `rg Reorder.Group src/features/createPreset/ui/PresetCompositionList.tsx` is at least 1
- `rg handleCategoryToggle src/features/createPreset/ui/CreatePreset.tsx` is 0
- `rg handleExerciseToggle src/features/createPreset/ui/CreatePreset.tsx` is 0
- `rg initialExercises src/pages/CreatePresetPage/ui/CreatePresetPage.tsx` still wires location.state
- `rg "/presets/create"|rg "/presets/edit" src/app/router/routes.tsx` unchanged
- No `*.module.css` under src/features/createPreset

Manual (non-blocking): open /presets/create, name a preset, add two catalog exercises via the drawer, drag to reorder, remove one, save; edit via /presets/edit?id=; from home AddExercise use create-from-current-workout and confirm the ordered list prefills.
</verification>

<success_criteria>
- Preset create/edit is composition-first: name + numbered ordered list (D-01, D-02, D-07)
- Add uses the existing catalog drawer/Command picker, not inline category checkboxes (D-03)
- Reorder uses ExerciseList motion Reorder pattern (D-04)
- Routes and initialExercises hydration unchanged (D-05, D-06)
- Store contract remains exercises: string[]
- Tailwind token classNames only; pnpm-only commands (D-08, D-09)
</success_criteria>

<output>
Create `.planning/quick/260815-q1b-rework-preset-create-edit-into-a-composi/260815-q1b-SUMMARY.md` when done
</output>
