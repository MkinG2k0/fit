# Quick task 260705-vpp

Подстановка веса и повторений из прошлой тренировки при включённой настройке «Показывать результат прошлой тренировки».

## Tasks

1. Расширить `findLastExerciseSession` — массив `sets` и хелпер `getSetPrefillFromLastSession`.
2. `ExerciseBody` — prefill первого подхода и «Добавить подход» по флагу `exerciseCardShowLastSessionResult`.
3. Обновить описание настройки в `ExerciseCardDisplaySettingsCard`.
