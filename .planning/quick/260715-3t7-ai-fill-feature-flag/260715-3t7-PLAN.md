---
phase: 260715-3t7-ai-fill-feature-flag
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/entities/user/slice/userStore.ts
  - src/features/appSettingsTransfer/lib/appSettingsSectionRegistry.ts
  - src/features/exercise/ui/AiFillSettingsCard.tsx
  - src/features/exercise/index.ts
  - src/pages/SettingsPage/ui/SettingsPage.tsx
  - src/features/exercise/ui/ExerciseBody.tsx
autonomous: true
requirements:
  - QUICK-3T7-01
  - QUICK-3T7-02
  - QUICK-3T7-03
must_haves:
  truths:
    - "В Настройках есть переключатель «ИИ-заполнение», по умолчанию выключен."
    - "Кнопка «ИИ-заполнение» в карточке упражнения видна только когда флаг включён; при выключенном флаге кнопки и её статус/ошибок нет."
    - "Значение флага сохраняется через persist userStore и участвует в экспорте/импорте профиля настроек."
  artifacts:
    - path: src/entities/user/slice/userStore.ts
      provides: "aiFillEnabled (default false) + setAiFillEnabled + merge boolean coerce"
    - path: src/features/exercise/ui/AiFillSettingsCard.tsx
      provides: "Settings card checkbox for AI fill feature flag"
    - path: src/features/exercise/ui/ExerciseBody.tsx
      provides: "Conditional render of AI fill button gated by aiFillEnabled"
    - path: src/features/appSettingsTransfer/lib/appSettingsSectionRegistry.ts
      provides: "aiFillEnabled in userProfile export/import"
  key_links:
    - from: "AiFillSettingsCard checkbox"
      to: "userStore.aiFillEnabled"
      via: "setAiFillEnabled"
    - from: "userStore.aiFillEnabled"
      to: "ExerciseBody AI fill Button"
      via: "useUserStore selector — render only when true"
    - from: "userStore.aiFillEnabled"
      to: "app settings transfer userProfile section"
      via: "exportSnapshot / importSnapshot / isUserProfileExport"
---

<objective>
Добавить фича-флаг «ИИ-заполнение» в настройки: по умолчанию OFF; кнопка в карточке упражнения показывается только при включённом флаге (D-01, D-02, D-03). Persist через userStore и участие в экспорте/импорте профиля (D-04).

Purpose: Скрыть экспериментальный AI fill от начинающих до явного включения в Настройках.
Output: Поле в userStore, карточка настроек, условный рендер кнопки, transfer registry.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@src/entities/user/slice/userStore.ts
@src/features/exercise/ui/WorkoutSummaryDisplaySettingsCard.tsx
@src/features/exercise/ui/RestBetweenSetsSettingsCard.tsx
@src/features/exercise/ui/ExerciseBody.tsx
@src/pages/SettingsPage/ui/SettingsPage.tsx
@src/features/appSettingsTransfer/lib/appSettingsSectionRegistry.ts
@src/features/exercise/index.ts
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Settings UI → userStore | Пользовательский boolean флаг (локальный) |
| Feature flag → AI fill network call | При выключенном флаге UI не открывает путь к AI API |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-3T7-01 | Information Disclosure | AI fill button always visible | medium | mitigate | Default `aiFillEnabled: false`; кнопка и сообщения об ошибках AI fill не рендерятся при false |
| T-3T7-02 | Tampering | persisted aiFillEnabled | low | mitigate | В merge и transfer принимать только boolean; иначе default false |
| T-3T7-03 | Elevation of Privilege | N/A (local flag) | low | accept | Флаг только UI-гейт; сетевые ключи AI уже gated существующим кодом feature |
</threat_model>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Persist aiFillEnabled в userStore + transfer</name>
  <files>src/entities/user/slice/userStore.ts, src/features/appSettingsTransfer/lib/appSettingsSectionRegistry.ts</files>
  <action>
Добавить фича-флаг в `useUserStore` по образцу `workoutListShowDaySummary` / `restBetweenSetsEnabled` (D-01, D-04):

1. В `UserState`: `aiFillEnabled: boolean` с default `false` (экспериментальная фича — выкл. до явного включения).
2. В `ActionsState`: `setAiFillEnabled: (enabled: boolean) => void`, реализация через `set(() => ({ aiFillEnabled: enabled }))`.
3. В `persist.merge`: если `typeof p.aiFillEnabled === "boolean"` — взять значение, иначе оставить `current.aiFillEnabled` (default false для старых persist-блобов без поля).
4. В `appSettingsSectionRegistry.ts` секции `userProfile` зеркально с другими boolean-флагами:
   - расширить типы `isUserProfileExport` / `exportSnapshot` state / `importSnapshot` prevState: optional `aiFillEnabled?: boolean`;
   - в `isUserProfileExport`: если ключ присутствует — требовать boolean;
   - в `exportSnapshot`: `aiFillEnabled: state.aiFillEnabled ?? false`;
   - в `importSnapshot` (оба пути — merge с existing и create new): `aiFillEnabled: payload.aiFillEnabled ?? prev/false` с тем же fallback-паттерном, что у `exerciseCardShowLastSessionResult`.

Не трогать accessToken. Не менять другие флаги.
  </action>
  <verify>
    <automated>pnpm exec tsc -b --pretty false 2>&amp;1 | Select-Object -Last 30</automated>
  </verify>
  <done>
    userStore имеет `aiFillEnabled: false` по умолчанию, setter и coalesce в merge; userProfile export/import переносит поле.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Карточка настроек ИИ-заполнение</name>
  <files>src/features/exercise/ui/AiFillSettingsCard.tsx, src/features/exercise/index.ts, src/pages/SettingsPage/ui/SettingsPage.tsx</files>
  <action>
Создать UI флага по паттерну `WorkoutSummaryDisplaySettingsCard.tsx` (D-02):

1. Новый файл `AiFillSettingsCard.tsx`: Card + Checkbox + Label; title «ИИ-заполнение»; description про экспериментальное автозаполнение подходов; label чекбокса «Показывать кнопку ИИ-заполнения»; hint — при выключении кнопка скрыта в карточке упражнения. Читать/писать `aiFillEnabled` / `setAiFillEnabled` из `useUserStore`. Иконка: `Sparkles` из lucide-react (или `Wand2` если Sparkles нет в проекте). Стили только Tailwind-токенами (`border-border`, `text-muted-foreground` и т.д.), `className` через `cn` как у соседних карточек. id чекбокса: `ai-fill-enabled`.
2. Экспорт из `src/features/exercise/index.ts` рядом с `RestBetweenSetsSettingsCard`.
3. В `SettingsPage.tsx`: импорт `AiFillSettingsCard`, вставить после `RestBetweenSetsSettingsCard` (или перед `ExerciseCardDisplaySettingsCard`) — логически рядом с экспериментальными/дисплейными тумблерами.
  </action>
  <verify>
    <automated>pnpm exec rg -n "AiFillSettingsCard|aiFillEnabled" src/pages/SettingsPage/ui/SettingsPage.tsx src/features/exercise/index.ts src/features/exercise/ui/AiFillSettingsCard.tsx</automated>
  </verify>
  <done>
    На странице настроек есть карточка «ИИ-заполнение» с чекбоксом, связанным со store; карточка экспортирована из feature barrel.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Скрыть кнопку ИИ-заполнение по флагу</name>
  <files>src/features/exercise/ui/ExerciseBody.tsx</files>
  <action>
В `ExerciseBody.tsx` (D-03): подписаться на `aiFillEnabled` через `useUserStore((s) => s.aiFillEnabled ?? false)`.

Рендер кнопки «ИИ-заполнение» (около строк с `variant="outline"` и текстом загрузки/«ИИ-заполнение») и связанные `aiFillEmptyMessage` / `aiFillError` блоки — только если `aiFillEnabled === true`. При `false` не показывать disabled-кнопку (полное скрытие).

Не удалять `handleAiFill` и логику загрузки — оставить за хэндора; достаточно UI-гейта. Не менять «Добавить подход» / delete / StatisticCard.
  </action>
  <verify>
    <automated>pnpm exec rg -n "aiFillEnabled" src/features/exercise/ui/ExerciseBody.tsx; pnpm exec tsc -b --pretty false 2>&amp;1 | Select-Object -Last 20</automated>
  </verify>
  <done>
    При `aiFillEnabled === false` в разметке карточки нет кнопки ИИ-заполнения и её статус-сообщений; при `true` — как сейчас.
  </done>
</task>

</tasks>

<verification>
- `pnpm exec tsc -b` проходит без ошибок по затронутым типам.
- Fresh persist: `aiFillEnabled` отсутствует в старых данных → coalesce к false → кнопка скрыта.
- Ручная проверка (executor в SUMMARY): Настройки → включить → кнопка появляется; выключить → исчезает без disabled-стейта.
</verification>

<success_criteria>
- Фича-флаг default OFF (D-01).
- Settings card «ИИ-заполнение» переключает видимость (D-02).
- Кнопка скрыта целиком при OFF (D-03).
- Persist + export/import профиля включают флаг (D-04).
</success_criteria>

<output>
Create `.planning/quick/260715-3t7-ai-fill-feature-flag/260715-3t7-SUMMARY.md` when done
</output>
