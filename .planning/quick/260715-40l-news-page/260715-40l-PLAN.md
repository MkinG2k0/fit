---
phase: 260715-40l-news-page
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/news/model/types.ts
  - src/features/news/model/newsEntries.ts
  - src/features/news/lib/groupNewsByWeek.ts
  - src/features/news/ui/NewsFeed.tsx
  - src/features/news/index.ts
  - src/pages/NewsPage/ui/NewsPage.tsx
  - src/pages/NewsPage/index.ts
  - src/pages/index.ts
  - src/app/router/routes.tsx
  - src/app/layout/AppLayout.tsx
  - src/features/profileDropDownMenu/ui/profileDropDownMenu.tsx
autonomous: true
requirements:
  - QUICK-40L-01
  - QUICK-40L-02
  - QUICK-40L-03
must_haves:
  truths:
    - "В меню профиля есть пункт «Новости», ведущий на /news."
    - "На странице «Новости» показаны пользовательские фичи из статичного changelog, сгруппированные по ISO-неделям (понедельник–воскресенье)."
    - "Недели отсортированы от новых к старым; внутри недели записи тоже от новых к старым."
    - "В браузере нет обращения к .git — только статичный модуль данных."
  artifacts:
    - path: src/features/news/model/newsEntries.ts
      provides: "Curated NewsEntry[] derived from git feat history"
    - path: src/features/news/lib/groupNewsByWeek.ts
      provides: "ISO-week grouping helper"
    - path: src/features/news/ui/NewsFeed.tsx
      provides: "Weekly news feed UI"
    - path: src/pages/NewsPage/ui/NewsPage.tsx
      provides: "News page composition"
  key_links:
    - from: "ProfileDropDownMenu «Новости»"
      to: "/news route"
      via: "navigate('/news')"
    - from: "AppLayout PAGE_TITLES['/news']"
      to: "Header title «Новости»"
      via: "resolvePageTitle"
    - from: "newsEntries"
      to: "NewsFeed sections"
      via: "groupNewsByWeek"
---

<objective>
Создать страницу «Новости» с changelog новых фич приложения: данные — статичный curated-модуль из `git log` (не live git в браузере), группировка по ISO-неделям, маршрут `/news` и пункт в меню профиля.

Purpose: Пользователь видит, что появилось в приложении, без шума chore/docs/fix.
Output: feature `news`, страница NewsPage, роут и навигация.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@src/app/router/routes.tsx
@src/app/layout/AppLayout.tsx
@src/pages/AiRecommendationsPage/ui/AiRecommendationsPage.tsx
@src/pages/AiRecommendationsPage/index.ts
@src/pages/index.ts
@src/features/profileDropDownMenu/ui/profileDropDownMenu.tsx
@src/widgets/weekCalendar/ui/Calendar.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Changelog data + ISO-week grouping</name>
  <files>src/features/news/model/types.ts, src/features/news/model/newsEntries.ts, src/features/news/lib/groupNewsByWeek.ts, src/features/news/index.ts</files>
  <action>
    Контракт groupNewsByWeek: пустой вход → []; записи одной ISO-недели → одна секция (weekStartDate = понедельник); разные недели → отдельные секции по weekStartDate убыв.; внутри секции entries по date убыв.
    1. Создать `NewsEntry`: `{ id: string; date: string /* YYYY-MM-DD */; title: string; summary: string }`.
    2. Создать `NewsWeekGroup`: `{ weekStartDate: string /* YYYY-MM-DD Monday */; label: string; entries: NewsEntry[] }`.
    3. Заполнить `newsEntries` **статично** по `git log`:
       - Команда для сбора: `git log --pretty=format:"%h|%ad|%s" --date=short --grep="^feat" -i --since="2026-04-01"`.
       - Включать только пользовательские фичи (feat/feature); пропускать chore/docs/fix/refactor/style и чисто технические коммиты (AndroidManifest receivers без UX, если нет user-facing смысла).
       - Серию коммитов одной фичи (например все `feat(260715-052-*)`) **объединять в одну запись** с человекочитаемым `title`/`summary` на русском.
       - Обязательно покрыть недавние user-facing фичи из истории: ИИ-заполнение + флаг в настройках, отдых между подходами / deadline-таймер, AI рекомендации, таблица нагрузок, слияние упражнений, префилл подходов из прошлой тренировки, фото/описание упражнений, настройки карточки/сводки, аналитика, параметры тела/активность — с реальными датами коммитов (не выдумывать).
       - Минимум ~12 curated entries за апрель–июль 2026; даты — день представительного коммита фичи (обычно финальный wire-коммит).
    4. Реализовать `groupNewsByWeek(entries)` через dayjs + plugin `isoWeek` (как в `src/widgets/weekCalendar/ui/Calendar.tsx`): `startOf("isoWeek")` = понедельник. `label` вида «14–20 июля 2026» (локаль ru) или эквивалент через dayjs format.
    5. Экспорт через `src/features/news/index.ts`: типы, `newsEntries`, `groupNewsByWeek`.
    6. Не читать `.git` в рантайме браузера. Не добавлять новые npm-зависимости.
  </action>
  <verify>
    <automated>pnpm exec tsc -b --pretty false</automated>
  </verify>
  <done>Статичный curated changelog и ISO-week группировка экспортируются из `@/features/news`; related feat-коммиты слиты в user-facing записи.</done>
</task>

<task type="auto">
  <name>Task 2: News page UI + route + nav</name>
  <files>src/features/news/ui/NewsFeed.tsx, src/pages/NewsPage/ui/NewsPage.tsx, src/pages/NewsPage/index.ts, src/pages/index.ts, src/app/router/routes.tsx, src/app/layout/AppLayout.tsx, src/features/profileDropDownMenu/ui/profileDropDownMenu.tsx</files>
  <action>
    1. `NewsFeed`: взять `newsEntries`, вызвать `groupNewsByWeek`, отрендерить секции. Заголовок недели (`label`) + список: дата записи + title + summary. Tailwind-токены (`bg-background`, `text-foreground`, `text-muted-foreground`, `border-border`); без CSS Modules; className inline / cn на месте. Без карточек-ради-карточек: секция = заголовок + список с разделителями (`Separator` из shadcn или border-token). Пустой список — короткий текст «Пока нет новостей».
    2. `NewsPage` по паттерну `AiRecommendationsPage`: тонкая обёртка `max-w-5xl` + короткий `text-muted-foreground` lead («Что нового в приложении») + компонент NewsFeed. Barrel `src/pages/NewsPage/index.ts`, реэкспорт в `src/pages/index.ts`.
    3. Роут: lazy `NewsPage` в `routes.tsx`, `path="/news"` внутри `ProtectedRoute` (как `/ai-recommendations`).
    4. `AppLayout` `PAGE_TITLES`: `"/news": "Новости"`.
    5. В `ProfileDropDownMenu` пункт «Новости» с иконкой `Newspaper` (lucide-react), `handleNavigate("/news")`, рядом с другими пунктами (логично после Аналитики или перед Настройками), Separator как у соседей.
  </action>
  <verify>
    <automated>pnpm exec tsc -b --pretty false</automated>
  </verify>
  <done>Маршрут /news открывает страницу с недельными группами фич; пункт меню и заголовок «Новости» подключены.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Static module → UI | Контент changelog полностью под контролем репозитория (нет user input) |
| Navigation → /news | Публичный client-side route без секретов |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-40L-01 | Information Disclosure | newsEntries | low | accept | Только user-facing описания; без токенов/API URL/внутренних путей |
| T-40L-02 | Tampering | Client static data | low | accept | Local-first SPA; пользователь и так контролирует клиент |
| T-40L-SC | Tampering | Package installs | low | accept | Новых пакетов нет — только существующие dayjs/lucide/react-router |
</threat_model>

<verification>
- `pnpm exec tsc -b --pretty false` проходит.
- Ручной smoke после execute: меню → Новости → видны группы по неделям с фичами load-table / AI / таймер и т.д.
</verification>

<source_audit>
| SOURCE | ID | Item | Plan | Status |
|--------|----|------|------|--------|
| GOAL | — | Страница Новости с фичами из git, группировка по неделям | 01 | COVERED |
| REQ | QUICK-40L-01 | Показать новые фичи приложения | 01 Task1+2 | COVERED |
| REQ | QUICK-40L-02 | Источник — git feat history → static module | 01 Task1 | COVERED |
| REQ | QUICK-40L-03 | Группировка по ISO-неделям | 01 Task1+2 | COVERED |
| CONTEXT | — | quick mode, no discuss decisions | — | N/A |
| RESEARCH | — | no research phase | — | N/A |
</source_audit>

<success_criteria>
- Существует `/news` с заголовком «Новости».
- Changelog статичен и сгруппирован по ISO-неделям (новые сверху).
- Пункт «Новости» в ProfileDropDownMenu работает.
- Нет live-доступа к git из браузера.
</success_criteria>

<output>
Create `.planning/quick/260715-40l-news-page/260715-40l-SUMMARY.md` when done
</output>
