---
phase: 260815-q7h-fix-sausage-icon-overlay-close-on-outsid
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/shared/lib/navigation/overlaySearchParams.ts
  - src/shared/lib/navigation/index.ts
  - src/shared/lib/navigation/useNavigateBack.ts
  - src/app/providers/AndroidBackNavigation.tsx
  - src/widgets/statisticCard/ui/statisticCard.tsx
  - src/features/exercise/ui/ExerciseCard.tsx
  - src/features/exercise/ui/ExerciseBody.tsx
  - src/features/addExercise/ui/AddExercise.tsx
  - src/features/createPreset/ui/AddPresetExercisesDrawer.tsx
autonomous: true
requirements:
  - QUICK-Q7H-01
  - QUICK-Q7H-02
  - QUICK-Q7H-03
estimate:
  tokens: 18000
  raw_tokens: 18000
  tasks: 3
  confidence: high
must_haves:
  truths:
    - "Клик по ChartColumnBig в развёрнутой карточке упражнения (и swipe-right) открывает Dialog статистики через PUSH search-параметра exercise-stats=<workoutExercise.id> на текущем pathname."
    - "Клик по оверлею, X и явный close закрывают Dialog через history pop (navigate(-1)); replace-delete только если запись открыли без своего PUSH (deep link / refresh)."
    - "Android Back при наличии overlay search-параметра делает navigate(-1) и не вызывает resolveBackPath / App.minimizeApp; карта parent-routes 260811-tmf не меняется."
  artifacts:
    - path: src/shared/lib/navigation/overlaySearchParams.ts
      provides: "OVERLAY_SEARCH_PARAM_KEYS, hasOverlaySearchParam, useOverlaySearchParam (PUSH open / POP close / replace-delete fallback)"
    - path: src/widgets/statisticCard/ui/statisticCard.tsx
      provides: "Dialog open derived from caller; overlay/X вызывают onOpenChange(false) без preventDefault"
    - path: src/app/providers/AndroidBackNavigation.tsx
      provides: "backButton: overlay param → navigate(-1), else resolveBackPath / minimize"
  key_links:
    - from: "ChartColumnBig / swipe-right"
      to: "?exercise-stats=<exercise.id>"
      via: "useOverlaySearchParam PUSH"
    - from: "overlay click / Dialog X / Android Back"
      to: "previous history entry without the param"
      via: "navigate(-1)"
    - from: "AndroidBackNavigation"
      to: "hasOverlaySearchParam(searchParams)"
      via: "useLocation pathname+search"
---

<objective>
Починить модалку статистики упражнения (иконка ChartColumnBig — «колбаски»): закрытие по клику вне диалога и закрытие системной кнопкой Назад через GET search-параметр на том же маршруте.

Purpose: сейчас Dialog живёт только в React state, оверлей не закрывает его надёжно, а Android Back после 260811-tmf игнорирует search и на `/` сворачивает приложение.
Output: общий overlay-search-param хук (PUSH/POP), одна модалка статистики на карточку, Android/Header Back попают overlay, карта parent-routes не трогается.
</objective>

<execution_context>
@C:/Users/mk/.cursor/gsd-core/workflows/execute-plan.md
@C:/Users/mk/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@src/widgets/statisticCard/ui/statisticCard.tsx
@src/features/exercise/ui/ExerciseCard.tsx
@src/features/exercise/ui/ExerciseBody.tsx
@src/features/addExercise/ui/AddExercise.tsx
@src/features/createPreset/ui/AddPresetExercisesDrawer.tsx
@src/app/providers/AndroidBackNavigation.tsx
@src/shared/lib/navigation/useNavigateBack.ts
@src/shared/lib/navigation/resolveBackPath.ts
@src/shared/ui/shadCNComponents/ui/dialog.tsx
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Shared overlay search-param hook (PUSH open, POP close)</name>
  <files>src/shared/lib/navigation/overlaySearchParams.ts, src/shared/lib/navigation/index.ts, src/features/addExercise/ui/AddExercise.tsx, src/features/createPreset/ui/AddPresetExercisesDrawer.tsx</files>
  <read_first>src/features/addExercise/ui/AddExercise.tsx, src/features/createPreset/ui/AddPresetExercisesDrawer.tsx, src/shared/lib/navigation/index.ts, src/shared/lib/navigation/useNavigateBack.ts</read_first>
  <action>
Confirmed target UI: sausage icon is lucide `ChartColumnBig` (three vertical bars) on the workout exercise card — NOT header `Menu`. This task only builds the shared history helper and migrates the existing `?add-exercise=1` drawers onto it so the stats dialog does not invent a second close strategy.

Create `src/shared/lib/navigation/overlaySearchParams.ts`:

- `ADD_EXERCISE_PARAM = "add-exercise"`
- `EXERCISE_STATS_PARAM = "exercise-stats"`
- `OVERLAY_SEARCH_PARAM_KEYS = [ADD_EXERCISE_PARAM, EXERCISE_STATS_PARAM] as const`
- `hasOverlaySearchParam(searchParams: URLSearchParams): boolean` — true if any key in OVERLAY_SEARCH_PARAM_KEYS is present (any value).
- `useOverlaySearchParam(paramKey: string, value: string)`:
  - `isOpen` = `searchParams.get(paramKey) === value`
  - `open()`: if already open, return. Else clone searchParams, `set(paramKey, value)`, `didPushRef.current = true`, `setSearchParams(next)` with default PUSH (no `{ replace: true }`). Same pathname, do not navigate to a parent route.
  - `close()`: if not open, return. If `didPushRef.current` is true: set it false and `navigate(-1)`. Else clone, `delete(paramKey)`, `setSearchParams(next, { replace: true })` (landed with param already in the URL — no matching history entry).
  - `onOpenChange(next: boolean)` → open() / close()
  - `useEffect`: when `isOpen` becomes false, set `didPushRef.current = false` so an external pop (Android Back / browser Back) does not leave the ref sticky and double-pop on the next close.
  - Do not encode extra history state objects. Do not call `resolveBackPath`.

Re-export from `src/shared/lib/navigation/index.ts`.

Migrate `AddExercise` and `AddPresetExercisesDrawer` off inline PUSH + replace-delete:

- Replace local `DRAWER_QUERY_PARAM` / `DRAWER_QUERY_VALUE` with `ADD_EXERCISE_PARAM` and value `"1"` via `useOverlaySearchParam(ADD_EXERCISE_PARAM, "1")`.
- `open={isOpen}`, `onOpenChange` from the hook (Drawer already calls onOpenChange on overlay dismiss).
- Submit / navigate-away close must go through `close()` (POP), not a handwritten replace-delete.
- Keep `useDrawerViewportStyle` and the rest of drawer UI unchanged.

Do not touch `analytics-exercise` or `calorieProfile`. Do not edit `resolveBackPath` / `PARENT_BY_PATH`.
  </action>
  <verify>
    <automated>rg -n "useOverlaySearchParam|EXERCISE_STATS_PARAM|ADD_EXERCISE_PARAM|hasOverlaySearchParam|replace: true" src/shared/lib/navigation src/features/addExercise/ui/AddExercise.tsx src/features/createPreset/ui/AddPresetExercisesDrawer.tsx; pnpm exec tsc -b --pretty false</automated>
  </verify>
  <done>
    Shared hook exists and is exported. Add-exercise drawers PUSH on open and POP on close (replace-delete only as no-push fallback). tsc -b passes.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Unify sausage stats Dialog: overlay click + exercise-stats param</name>
  <files>src/widgets/statisticCard/ui/statisticCard.tsx, src/features/exercise/ui/ExerciseCard.tsx, src/features/exercise/ui/ExerciseBody.tsx</files>
  <read_first>src/widgets/statisticCard/ui/statisticCard.tsx, src/features/exercise/ui/ExerciseCard.tsx, src/features/exercise/ui/ExerciseBody.tsx, src/shared/ui/shadCNComponents/ui/dialog.tsx, src/features/exercise/ui/ExerciseNameSelector.tsx</read_first>
  <action>
There are currently TWO `StatisticCard` instances per exercise: trigger+uncontrolled Dialog inside `ExerciseBody` (unmounts when the card collapses) and a controlled Dialog on `ExerciseCard` for swipe-right. Unify to one Dialog that always stays mounted on `ExerciseCard`.

`ExerciseCard`:
- `const statsOverlay = useOverlaySearchParam(EXERCISE_STATS_PARAM, exercise.id)` (workout `Exercise.id`, not catalogExerciseId — duplicates of the same catalog entry must not share one param).
- Swipe-right: `statsOverlay.open()` instead of `setIsStatisticOpen(true)`. Remove `isStatisticOpen`.
- Keep the single `StatisticCard` at card level: `open={statsOverlay.isOpen}` `onOpenChange={statsOverlay.onOpenChange}` `showTrigger={false}`.
- Pass `onOpenStats={statsOverlay.open}` into `ExerciseBody`.

`ExerciseBody`:
- Remove the inner `StatisticCard`.
- In the same footer slot, render the sausage trigger as a local `Button variant="outline" size="icon"` with `ChartColumnBig` and `aria-label="Статистика"`, `type="button"`, `onClick={onOpenStats}`. Do not wrap it in DialogTrigger. Tailwind tokens only (`text-muted-foreground` on the icon if it already uses that elsewhere; match the previous StatisticCard trigger classes).

`StatisticCard`:
- Stay controlled via `open` / `onOpenChange`. Keep chart loading, tabs, and nested fullscreen photo Dialog as-is (photo has no search param; Back closing the whole stats modal is accepted).
- Overlay dismiss MUST call `onOpenChange(false)`:
  - Do not `preventDefault` on `onPointerDownOutside` / `onInteractOutside`.
  - If Radix does not fire `onOpenChange` for overlay clicks, add handlers on `DialogContent` that close when the event target is the overlay (`data-slot="dialog-overlay"` — this is what `dialog.tsx` sets; do not copy ExerciseNameSelector’s `data-radix-dialog-overlay` check unless that attribute is actually present).
  - Do not change the global `dialog.tsx` primitive unless overlay dismiss is broken for this Dialog after the handlers above; this task is not a rewrite of every dialog.
- X (`DialogPrimitive.Close`) already goes through `onOpenChange`; keep it.
- Do not leave `isStatisticOpen` / internal open state fighting the URL. Uncontrolled `internalDialogOpen` can remain only as fallback when `open` is omitted; both call sites in this task are controlled.

Do not restyle the Dialog. No CSS modules. No parent-route navigation when opening stats.
  </action>
  <verify>
    <automated>rg -n "EXERCISE_STATS_PARAM|onOpenStats|isStatisticOpen|ChartColumnBig" src/features/exercise/ui/ExerciseCard.tsx src/features/exercise/ui/ExerciseBody.tsx src/widgets/statisticCard/ui/statisticCard.tsx; pnpm exec tsc -b --pretty false</automated>
  </verify>
  <done>
    One stats Dialog per card, keyed by workout exercise.id. Trigger button and swipe-right PUSH `?exercise-stats=id`. Overlay click, X, and onOpenChange(false) POP (or replace-delete fallback). ExerciseBody no longer mounts its own Dialog. tsc -b passes.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Android/Header Back pops overlay params instead of parent-route/minimize</name>
  <files>src/app/providers/AndroidBackNavigation.tsx, src/shared/lib/navigation/useNavigateBack.ts</files>
  <read_first>src/app/providers/AndroidBackNavigation.tsx, src/shared/lib/navigation/useNavigateBack.ts, src/shared/lib/navigation/resolveBackPath.ts, src/shared/lib/navigation/overlaySearchParams.ts</read_first>
  <action>
`AndroidBackNavigation` currently reads only `pathname` and on `/` calls `App.minimizeApp()` even if a modal is open. After 260811-tmf it must NOT go back to blanket `history.back()` for real pages.

Change the `backButton` handler:
1. Read `search` from `useLocation()` (add `search` to the effect deps). Build `URLSearchParams`.
2. If `hasOverlaySearchParam(searchParams)`: `navigate(-1)` and return. Do not `resolveBackPath`. Do not `App.minimizeApp()`.
3. Else keep the existing parent-route map: `resolveBackPath(pathname)` → `navigate(backPath)`, else `App.minimizeApp()`.

`useNavigateBack` (Header arrow): if overlay param is present, `navigate(-1)` and return true; else keep `resolveBackPath`. Home Header usually has no back arrow, but this keeps Header consistent if stats/add-exercise ever sit on a nested page.

Do not edit `PARENT_BY_PATH` or `ROOT_PATHS`. Do not pop when no overlay param is present.
  </action>
  <verify>
    <automated>rg -n "hasOverlaySearchParam|navigate\(-1\)|resolveBackPath|minimizeApp" src/app/providers/AndroidBackNavigation.tsx src/shared/lib/navigation/useNavigateBack.ts src/shared/lib/navigation/resolveBackPath.ts; pnpm exec tsc -b --pretty false</automated>
    <human-check>
      На главной: добавить упражнение, развернуть карточку, нажать ChartColumnBig — URL остаётся `/` плюс `?exercise-stats=&lt;id&gt;`. Клик по затемнению закрывает модалку и убирает параметр, страница не меняется. X закрывает так же. Android/browser Back закрывает модалку, не уходит со страницы и не сворачивает приложение. Повторный Back на `/` без параметра по-прежнему сворачивает приложение. Swipe-right открывает ту же модалку с тем же параметром. Drawer «Добавить упражнение» (`?add-exercise=1`): overlay/Back закрывают его через pop, без перехода на другой route.
    </human-check>
  </verify>
  <done>
    Overlay params pop via navigate(-1) from Android Back and Header back. Unrelated screens still use resolveBackPath / minimize. tsc -b passes.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL search string → Dialog open | User/history can put `exercise-stats` / `add-exercise` in the address bar |
| Android backButton plugin → SPA history | Native back must not skip overlay or minimize while a modal param is present |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-Q7H-01 | Spoofing | `exercise-stats` value | low | mitigate | Compare to workout `Exercise.id` already on the page; unknown id → Dialog stays closed (isOpen false). Do not eval the value. |
| T-Q7H-02 | Denial of Service | Repeated PUSH without POP | low | mitigate | `open()` no-ops if already open; `didPushRef` resets when `isOpen` becomes false. |
| T-Q7H-03 | Tampering | Parent-route map bypass | medium | mitigate | `navigate(-1)` only when `hasOverlaySearchParam`; otherwise 260811-tmf map unchanged. |
</threat_model>

<verification>
- `pnpm exec tsc -b` passes.
- `rg` shows `useOverlaySearchParam` used by stats card flow and add-exercise drawers; AddExercise no longer replace-deletes on the happy path.
- `AndroidBackNavigation` and `useNavigateBack` call `hasOverlaySearchParam` before `resolveBackPath`.
- `PARENT_BY_PATH` in `resolveBackPath.ts` is unchanged.
- Human-check: overlay click + Android Back close the sausage Dialog without leaving `/`.
</verification>

<success_criteria>
- QUICK-Q7H-01: клик вне модалки статистики закрывает её (и сбрасывает visual state триггера); X по-прежнему закрывает.
- QUICK-Q7H-02: открытие пишет GET-параметр `exercise-stats` на текущем route (PUSH); close/Back делают POP, страница не меняется.
- QUICK-Q7H-03: Android Back при открытой overlay-модалке не minimize и не идёт по parent-route map; без overlay-параметра поведение 260811-tmf сохраняется.
- Не рефакторить все Dialog/Drawer приложения; `analytics-exercise` / `calorieProfile` вне скоупа.
</success_criteria>

<output>
Create `.planning/quick/260815-q7h-fix-sausage-icon-overlay-close-on-outsid/260815-q7h-SUMMARY.md` when done
</output>
