---
phase: 260715-2o1-ai
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/aiRecommendations/model/types.ts
  - src/features/aiRecommendations/lib/prompts.ts
  - src/features/aiRecommendations/lib/recommendationStorage.ts
  - src/features/aiRecommendations/ui/AiRecommendationsPanel.tsx
  - src/features/aiRecommendations/index.ts
autonomous: true
requirements:
  - QUICK-2O1-01
  - QUICK-2O1-02
  - QUICK-2O1-03
must_haves:
  truths:
    - "User can pick a recommendation kind (next_session, progression, technique, recovery, custom) independently of period."
    - "Textarea «Ваш запрос» is always visible; required when kind is custom (Russian validation, no API call); optional otherwise and appended as clarification when filled."
    - "Saved recommendations load/save under composite key period:kind; switching period or kind shows that combo’s saved content; empty workout log still blocks API for every kind."
    - "User prompt includes period label, kind label, kind-specific Russian focus directive, workout log, optional custom query; markdown-friendly structure requested."
  artifacts:
    - path: src/features/aiRecommendations/model/types.ts
      provides: "AiRecommendationKind union + labels helpers alongside existing periods"
    - path: src/features/aiRecommendations/lib/recommendationStorage.ts
      provides: "Composite-key map Record<string, SavedRecommendation>"
    - path: src/features/aiRecommendations/lib/prompts.ts
      provides: "buildUserPrompt(periodLabel, kindLabel, workoutLogText, customQuery?)"
    - path: src/features/aiRecommendations/ui/AiRecommendationsPanel.tsx
      provides: "Period + kind radios, textarea, request/validation UX"
  key_links:
    - from: "period + kind state"
      to: "loadSavedRecommendation / saveRecommendation"
      via: "storage key `${period}:${kind}`"
    - from: "kind + customQuery + workoutLogText"
      to: "createChatCompletion messages"
      via: "getSystemPrompt + buildUserPrompt"
---

<objective>
Расширить AI рекомендации: тип запроса (radio) + всегда видимый textarea «Ваш запрос», композитное сохранение `period:kind` и kind-aware user prompt (D-01…D-06).

Purpose: Новичок получает целевой совет (следующая сессия / прогресс / техника / восстановление / свой запрос) по своему журналу.
Output: Обновления только в `src/features/aiRecommendations/` (+ native textarea по паттерну проекта).
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@src/features/aiRecommendations/ui/AiRecommendationsPanel.tsx
@src/features/aiRecommendations/model/types.ts
@src/features/aiRecommendations/lib/prompts.ts
@src/features/aiRecommendations/lib/recommendationStorage.ts
@src/features/aiRecommendations/index.ts
@src/features/createExercise/ui/CreateExerciseDescriptionField.tsx
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user customQuery → AI gateway | Free-text user input is concatenated into the user prompt and leaves the device |
| localStorage map → UI | Previously saved assistant text keyed by period:kind |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-2o1-01 | Tampering | customQuery in buildUserPrompt | low | mitigate | Keep trainer system prompt; treat custom text as clarification only; still require non-empty workout log before API |
| T-2o1-02 | Information Disclosure | workout log + customQuery in request body | low | accept | Same local-gateway personal-data model as 260715-2az; no new outbound hosts |
| T-2o1-03 | Elevation of Privilege | storage key collision | low | mitigate | Composite keys use `${period}:${kind}` with known enums only; ignore legacy period-only map entries without rewriting them as valid new keys |
| T-2o1-SC | Tampering | package installs | high | accept | No new packages in this quick task |
</threat_model>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Kinds, prompts, composite storage</name>
  <files>src/features/aiRecommendations/model/types.ts, src/features/aiRecommendations/lib/prompts.ts, src/features/aiRecommendations/lib/recommendationStorage.ts, src/features/aiRecommendations/index.ts</files>
  <action>
Implement model + prompt + persistence only (D-01, D-03, D-04). Do not change the panel UI in this task.

types.ts (D-01):
- Add `AiRecommendationKind` union: `next_session | progression | technique | recovery | custom`.
- Add `AI_RECOMMENDATION_KINDS` array with Russian labels exactly: «Следующая тренировка», «Прогресс весов и повторов», «Техника», «Восстановление и частота», «Свой запрос».
- Add `getKindLabel(kind)` mirroring `getPeriodLabel`.

prompts.ts (D-04):
- Keep `getSystemPrompt` as beginner trainer (existing Russian guidance; do not strip medical disclaimer or markdown-friendly expectation from the overall prompt contract).
- Change `buildUserPrompt` to accept `periodLabel`, `kindLabel`, `workoutLogText`, and optional `customQuery` (string | undefined).
- In the user prompt include: period line, kind line, a short Russian kind-specific focus directive (what to emphasize for that kind), the workout journal block, and — when `customQuery` is non-empty after trim — a clarification section with the user’s text.
- Kind focus directives (short, actionable Russian): next_session → план ближайшей сессии (упражнения/веса/повторы); progression → прогресс рабочих весов и повторов; technique → техника и типичные ошибки новичков; recovery → восстановление и частота; custom → опирайся на «Свой запрос» пользователя, журнал — контекст.
- Still ask for markdown-friendly structure (headings/lists) for MD rendering in UI.
- Empty journal is not handled here (caller still blocks) — do not special-case custom-only without logs (D-06).

recommendationStorage.ts (D-03):
- Replace period-only `Partial&lt;Record&lt;AiRecommendationPeriod, …&gt;&gt;` with `Record&lt;string, SavedRecommendation&gt;` (or Partial thereof).
- Export helper e.g. `recommendationStorageKey(period, kind)` → `` `${period}:${kind}` ``.
- `loadSavedRecommendation(period, kind)` / `saveRecommendation(period, kind, content)` read/write that composite key under existing `STORAGE_KEY` (`ai-recommendations`).
- On load of the whole map: if a top-level entry’s key has no `:` (legacy period-only shape), ignore it for lookups — no migration required; do not treat legacy keys as valid composite keys.
- On successful save: overwrite only the composite key; leave other keys untouched.

index.ts:
- Re-export new kind type, `AI_RECOMMENDATION_KINDS`, `getKindLabel` alongside existing period exports. Update any storage/prompt re-exports only if already exported; keep barrel consistent.
  </action>
  <verify>
    <automated>pnpm exec tsc -b --pretty false; rg -n "AiRecommendationKind|recommendationStorageKey|customQuery" src/features/aiRecommendations/model/types.ts src/features/aiRecommendations/lib/prompts.ts src/features/aiRecommendations/lib/recommendationStorage.ts</automated>
  </verify>
  <done>
    Kind union + labels exist; buildUserPrompt takes kind + optional customQuery with kind focus lines; storage load/save uses `${period}:${kind}` and ignores legacy period-only keys.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Panel radios, textarea, wire request</name>
  <files>src/features/aiRecommendations/ui/AiRecommendationsPanel.tsx</files>
  <read_first>src/features/createExercise/ui/CreateExerciseDescriptionField.tsx</read_first>
  <action>
Wire UI + request flow (D-01, D-02, D-05, D-06). Scope: this panel file only.

State (D-01, D-05):
- Keep period radios as today.
- Add `kind` state defaulting to `next_session` and a second RadioGroup (separate from period) with Russian labels from `AI_RECOMMENDATION_KINDS`. Place kind group clearly relative to period (period first, then kind — or kind then period; prefer period then kind then textarea for scan order).
- Add `customQuery` string state for the textarea. Do not persist the textarea text to storage (only assistant `content` is persisted).

Textarea (D-02):
- Always show labeled «Ваш запрос». shadcn Textarea component is not in the repo — use native `&lt;textarea&gt;` with the same Tailwind token classes as `CreateExerciseDescriptionField` (border-input, bg-background, placeholder:text-muted-foreground, etc.). No CSS modules; no `const` holding className strings outside JSX.
- Russian placeholder (e.g. clarify focus or ask a custom question).
- When `kind === "custom"` and user clicks request with empty/whitespace query: set a Russian validation message (role=alert or inline error), do not call the gateway.
- When kind is not custom: textarea optional; if filled, pass trimmed text into `buildUserPrompt` as customQuery.

Load/save (D-03):
- `useEffect` depends on both `period` and `kind`; call `loadSavedRecommendation(period, kind)` and set `result` accordingly; clear empty/error on switch as today.
- After successful completion content: `saveRecommendation(period, kind, content)`.

Request gates (D-06):
- Keep empty workout-log skip for all kinds (including custom): same Russian empty-period message, no API call.
- Then custom-required validation (if applicable).
- Then `buildUserPrompt(getPeriodLabel(period), getKindLabel(kind), workoutLogText, optionalCustom)` + existing `getSystemPrompt` / `createChatCompletion`.

Buttons (D-05):
- Keep loading / «Получить рекомендации» / «Обновить рекомендации» pattern based on whether current combo has saved/displayed result.
  </action>
  <verify>
    <automated>pnpm exec tsc -b --pretty false; rg -n "AI_RECOMMENDATION_KINDS|Ваш запрос|loadSavedRecommendation\(|saveRecommendation\(|buildUserPrompt\(" src/features/aiRecommendations/ui/AiRecommendationsPanel.tsx</automated>
  </verify>
  <done>
    Panel exposes period + kind radios and always-visible query textarea; custom requires non-empty query; persistence and prompts use period+kind; empty journal still blocks all kinds; button labels unchanged in pattern.
  </done>
</task>

</tasks>

<verification>
- `pnpm exec tsc -b` passes.
- Manual spot-check: switch period and kind independently → saved results do not bleed across combos; custom without text blocked; non-custom with filled textarea still requests; empty period still shows journal-empty message.
</verification>

<success_criteria>
- Five recommendation kinds selectable with locked Russian labels (D-01).
- Textarea always visible; required only for custom; optional clarification otherwise (D-02).
- Storage keyed by `period:kind`; legacy period-only entries ignored (D-03).
- User prompt carries kind focus + optional custom query; system trainer prompt retained (D-04).
- Empty workout log blocks API for every kind (D-06).
</success_criteria>

<output>
Create `.planning/quick/260715-2o1-ai/260715-2o1-SUMMARY.md` when done
</output>
